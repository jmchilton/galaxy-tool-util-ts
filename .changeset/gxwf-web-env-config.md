---
"@galaxy-tool-util/gxwf-web": minor
---

Add `GXWF_BACKEND_URL` and `GXWF_UI_DIST` env var support. `GXWF_BACKEND_URL` overrides the Vite dev proxy target (default `http://localhost:8000`). `GXWF_UI_DIST` overrides the bundled `public/` UI directory at runtime.
