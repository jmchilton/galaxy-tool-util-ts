---
---

Regenerate `parameter_models/*.json` goldens from current Galaxy `tool_parsing_abstraction` — closes the drift flagged in #158 (`data_ref` on data_column, `url_default` on data, `tabular` extensions + `truevalue`/`falsevalue` now matching the live fixtures). Test-fixture-only; no published-package change.
