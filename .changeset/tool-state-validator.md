---
"@galaxy-tool-util/schema": minor
---

Add `ToolStateValidator` class — high-level bridge for validating tool_state in workflow steps without exposing Effect internals. Supports both native and format2 step validation, returning `ToolStateDiagnostic[]`.
